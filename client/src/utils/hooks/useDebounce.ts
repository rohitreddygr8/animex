import { useEffect, useState } from "react";

export default function useDebounce({ value, delay }: { value: any; delay: number }) {
  //   const [val, setVal] = useState(value);
  //   useEffect(() => {}, [value]);
}
